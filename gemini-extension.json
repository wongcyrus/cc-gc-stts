{
  "name": "stts",
  "description": "Talk to the model via gemini cli and hear the model response back.",
  "version": "1.0.0",
  "mcpServers": {
    "stts": {
      "command": "node",
      "args": [
        "${extensionPath}/dist/stts-mcp-server.mjs"
      ],
      "cwd": "${extensionPath}"
    }
  }
}